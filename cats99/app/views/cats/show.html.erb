<h1>Show Page</h1>


<table>
    <thead>
        <tr>
            <th colspan="2">Cat Name</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= @cat.name %></td>
            <td><%= @cat.color %></td>
            <td><%= @cat.birth_date %></td>
            <td><%= @cat.sex %></td>
            <td><%= @cat.description %></td>
        </tr>
    </tbody>
</table>